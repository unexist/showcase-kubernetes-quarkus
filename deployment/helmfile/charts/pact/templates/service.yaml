apiVersion: v1
kind: Service
metadata:
  name: {{ include "pact.fullname" . }}
  labels:
    {{- include "pact.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.container.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "pact.selectorLabels" . | nindent 4 }}
